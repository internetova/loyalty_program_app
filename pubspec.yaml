name: loyalty_program_app
description: test task loyalty program

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.3
  flutter_svg: ^0.22.0
  intl: ^0.17.0

  flutter_bloc: ^7.0.0
  equatable: ^2.0.2

  provider: ^5.0.0
  shared_preferences: ^2.0.6

  moor: ^4.3.2
  sqlite3_flutter_libs: ^0.5.0
  path_provider: ^2.0.2
  path: ^1.8.0

  mask_text_input_formatter: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  moor_generator: ^4.3.1
  build_runner: ^2.0.4

flutter:

  uses-material-design: true

  assets:
    - res/
    - res/svg/
